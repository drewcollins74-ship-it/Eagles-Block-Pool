# Source

Website source code should go here.

Codex build note: create reusable components for the hero, call-to-action buttons, block-pool grid, how-it-works cards, and support-the-cause section. The weekly pool grid should render from `data/current-pool.csv` or a future JSON data file.
